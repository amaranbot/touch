# CLAUDE.md — Persistent Memory & Project Context

This file is my long-term memory. The session container is wiped between runs,
so anything worth keeping lives here, committed to the repo. I read this at the
start of every session. (Inspired by OpenClaw's local Markdown "vault" idea.)

---

## Project: Touch

A product landing page. Tagline: "Touch — Send a message today, deliver it to
someone's heart in the future." Single static `index.html` (inline CSS), warm
cream/gold serif aesthetic.

- `index.html` — the landing page (all styles inline)
- `README.md` — one-line description

---

## ⚠️ Privacy note — this repo is PUBLIC

Do NOT store personal data (health, schedule, contacts, email, accounts) in
this file. Anyone can read it. Personal/private memory belongs in a
self-hosted, private agent (OpenClaw or Hermes Agent), not a public repo.
This file is limited to project context only.

## Standing instructions / routines (project-scoped, non-sensitive)

<!-- Recurring dev/project tasks only. Nothing personal. -->

## Memory log

- 2026-06-07 — User asked for "permanent intelligence" like Hermes Agent /
  OpenClaw. Clarified those are self-hosted, always-on, private daemons —
  an architecture Claude Code on the web cannot replicate (ephemeral container,
  public repo, dormant between events). Recommended self-hosting the real thing.
  Scrubbed personal data that was initially (mistakenly) added here.
